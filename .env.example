# ── Required ───────────────────────────────────────────────────────────────────

# OpenAI API key — get yours at https://platform.openai.com/api-keys
OPENAI_API_KEY=
# OpenAI model — optional, default is gpt-4.1-mini (with Sanity, set this in the Profile document instead)
OPENAI_MODEL=

# ── Sanity CMS ─────────────────────────────────────────────────────────────────
# Project ID from sanity.io/manage — required in production
SANITY_PROJECT_ID=
# Dataset name (default: production)
SANITY_DATASET=production

# ── Local dev fallback (used only when SANITY_PROJECT_ID is not set) ───────────
# These values are served by /config.js when running without Sanity.
OWNER_NAME=Your Name
OWNER_TITLE=Your Title
LINKEDIN_URL=https://www.linkedin.com/in/yourprofile/
WEBSITE_URL=https://yoursite.example.com/
# Pipe-separated list of suggestion chips shown in the UI
SUGGESTIONS=Question 1|Question 2|Question 3

# ── Optional — Pushover notifications ──────────────────────────────────────────
# Sends mobile alerts when visitors leave contact details or ask unknown questions.
# Get your token/user key at https://pushover.net
PUSHOVER_TOKEN=
PUSHOVER_USER=
